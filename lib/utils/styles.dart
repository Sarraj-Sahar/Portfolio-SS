import 'package:flutter/material.dart';
import 'colors.dart';

myTextStyle(myfontSize) => TextStyle(
      color: myPurpleAccentColor,
      fontWeight: FontWeight.w500,
      fontFamily: "RobotoMono",
      fontSize: myfontSize,
    );
